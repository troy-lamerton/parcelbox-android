<?xml version="1.0" encoding="utf-8"?>
<resources>


    <!-- ## BoldButton with colored background ## -->
    <style name="BoldButton" parent="@style/Base.Widget.AppCompat.Button.Borderless.Colored">
        <item name="android:background">@drawable/bold_button_bg</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:paddingTop">@dimen/boldbutton_paddingTop</item>
        <item name="android:paddingBottom">@dimen/boldbutton_paddingBot</item>
        <item name="android:textSize">@dimen/fontsizeLarge</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- ## LightButton with transparent background ## -->
    <style name="LightButton" parent="@style/Base.Widget.AppCompat.Button.Borderless.Colored">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:textSize">@dimen/fontsizeDefault</item>
        <item name="android:lines">1</item>
    </style>

</resources>